@{
    ViewData["Title"] = "Properties";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3 mb-0">Properties</h1>
    <a href="#" class="btn btn-primary" data-modal-url="@Url.Action("Create")">Add property</a>
</div>

<div class="pm-panel" data-refresh-url="@Url.Action("List")">
    @await Component.InvokeAsync("PropertyList")
</div>
